import { OrdenarSeriesPipe } from './ordenar-series.pipe';

describe('OrdenarSeriesPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdenarSeriesPipe();
    expect(pipe).toBeTruthy();
  });
});
